# Retrospective — S10..S19 + Phase 12 (2026-08-30 .. 2026-09-01)

Per-sprint verdicts, all on independent-arbiter CLOSED_GO records (commit shown
is the CLOSED_GO commit).

| Scope | Verdict | CLOSED_GO commit |
|---|---|---|
| S10 controlled execution | GO | `e2bad67` chain / `05eefea` finalize-note |
| S11 rewrite stabilize + deliver | GO | `4876a49` |
| S12 go-plane integration + coverage | GO | `462fcd6` |
| Phase 12 legacy chat cutover | GO | `30f21e8` |
| S13 orca projection + preview | GO | `192aec5` |
| S14 CI/dev-loop | GO | `d09d5db` |
| S15 canonical adoption + DTO alignment | GO (after reviewer HIGHs fixed) | `7033a2a` |
| S16 canonical-default rollout + legacy freeze | GO | `b7a64d7` |
| S17 packaging | NO_GO then GO (5 blockers remediated) | `9155fb8` |
| S18 observability | GO | `b3330ed` |
| S19 shell rollout (host-local) | GO | `4d15528` |

## Risks remaining (checked 2026-09-01)

- **Observability, first real signal = daemon DOWN.** The installed probe task
  (`NEWSOS-S18-SLO-Probe`) is live and writing `slo.jsonl`, but the latest rows
  report `healthz: 000`, `consec_fails: 24` — because no sen-plane daemon runs
  persistently. Baseline after install = "canonical daemon not running". Action:
  run `scripts/run-s17.ps1 -Mode Native` (or a scheduled daemon) for the probe to
  measure up-time; decide whether daemon-always-on is desired.
- **Shell production deploy gated** — S19 enabled only on this dev host; any
  broader flip stays owner-decided (1809-gate).
- **CI container jobs** (`container-smoke`, `canonical-smoke`) need a
  Docker-capable runner; not exercised on this machine.
- **Backup cadence** verified twice (9/9 + 9/9); daily schedule automation is an
  ops follow-up outside repo.

## Repair update (2026-09-02)

The historical DOWN observation above was repaired without changing roadmap or
release authority. `NEWSOS-S17-SEN-PLANE` now owns the local loopback daemon,
S18 observes healthy `healthz: 200` samples, and the rollback/recovery evidence
plus an independent committed-byte `REVIEWED PASS` are recorded in
`plans/reports/s17-s18-runtime-repair-receipt-260901.md` and
`plans/reports/s17-s18-runtime-repair-independent-review-260901.md`.

## Lessons worth keeping

Freeze-not-delete for legacy writers (410 guard); fail-closed offline; DTO from
source-of-truth (never fabricated ids); single watch-loop probes; close pattern
(independent arbiter on committed bytes → CLOSED_GO → journal) proven across 10
sprints + a cutover.
