---
name: token-stack
description: Unified router for the 7-Layer Master Token & Context Engine (Code Topology, Ponytail, Caveman, RTK, Headroom, MemoraX, OpenViking).
---

# Token Stack Router

Provides access to the 7-Layer Token & Context Engine:
- **Layer 0**: Code Topology (`Graphify`, `GitNexus`, `CodeGraph`)
- **Layer 1**: Code Reduction (`Ponytail`)
- **Layer 2**: Word Reduction (`Caveman`)
- **Layer 3**: CLI Output Filter (`RTK`)
- **Layer 4**: Context Proxy (`Headroom`)
- **Layer 5**: Knowledge Harvester (`MemoraX Code`)
- **Layer 6**: Context Database Platform (`OpenViking`, `Obsidian Vault`)

## Sub-Skills
- `token-stack:benchmark` -> Run isolated and cumulative token savings benchmarks across standardized scenarios.
- `token-stack:health` -> Run live health probes across all 7 layers.
- `token-stack:setup` -> Configure and install components into profiles.
- `token-stack:report` -> Generate token savings analytics.