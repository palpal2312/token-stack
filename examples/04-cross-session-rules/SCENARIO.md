# Scenario 4: Cross-Session Rule & Experience Recall

## Objective
Evaluate token consumption when an agent starts a new session and must adhere to 5 strict project conventions (e.g. UTF-8 No BOM, ClickHouse DB, port dynamic scan) with vs without Layer 5 MemoraX Harvester.